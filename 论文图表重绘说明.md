# 图表内容重绘说明

## 1. 修改对象

原先的“美化版”主要在旧图外增加卡片边框与大标题，本次完全弃用该做法。
从冻结的实算结果重新创建Matplotlib图形对象：坐标、曲线、点、柱、阶梯面、热图、资源区间及地形图。
字体、颜色、线宽、图例位置和注释层级统一；不让装饰占据数据区域。

## 2. 科学编码的变化

需求分布不再只有单色总柱，改为四类物资的堆叠柱，并核对各柱总箱数；
连续与整箱可实现载荷用双端点/连接线突出差距；全服务区能力用真实45项色块和原值标注；
节点地图叠加原DEM、真实航段方向和固定机型颜色，不画不存在的行政分区或热力扩散；
返航SOC以显式20%底线、实点和区间线表达，不人为增强误差；
甘特图按机型或任务组赋色，以斜线区分充电/周转；
通信图使用最终结果中的完整正长度区间，边界仍由原验证器证书保证，不靠像素分辨率证明连续性。

## 3. 与参考图的对应及不能照搬的部分

- 能耗三维曲面：实际调用原模型的能耗函数，网格由81个载荷与61个海拔组成；保持真实S008航线距离和端点。
  其意义是条件模型响应，不是额外实际飞行，也不作为改变题定巡航规则后的提交方案。
- 小提琴：采用C型在20%、25%、30%、35%余量下每档15个服务区的真实计算值。
  核密度只作为轮廓，Scott带宽，裁在该档真实最小/最大值；全部样本实点保留。
  服务区并非随机独立重复试验，四分位线不是置信区间。载荷上限导致的大量80kg重合必须保留。
- 载荷—航程：沿用25/30/80kg额定载质量和题定指数关系，不照搬参考图中几百至几千kg的横轴。
- 需求空间分布：附件只有15个服务节点，本图只用点面积、点颜色和真实地形，不制造无观测的连续热力场。
- 多目标点图：有5个Q3已验证候选，就画这5个记录；不使用随机数补出上千点，不标为全球帕累托前沿。
- 状态转换：使用真实Q3-T-016的逐阶段记录、充电周期及通信区间。模型SOC的返航值等于源结果。

## 4. 对正文的影响

本次没有重新求解优化器。原始数据、清洗数据、四问数值文件和最终提交工作簿保留。
全部结果宏、数值表与公式保持不变；仅两条图注调整与图形实际含义一致：
一是需求柱的颜色进一步表示物资类别，二是中继高度图仅称飞行与服务剖面，不再错称含地形剖面。
Q1模型内精确性、Q2/Q3未证全局最优及Q4需要分类型资源增补的限定，仍然保留。

## 5. 交付与验证解释

163幅独立图中147幅是原图编号的一对一重画，16幅是对既有数据或既有物理模型的补充诊断。
6幅多面板组合图只是这些独立图的矢量排版，不重复算成6次实验。
逐图真实数据、来源哈希、KDE统计、能耗曲面网格、SOC阶段增量均可在plot_data中审查。
本轮验证是绘图与源数值的一致性验证；完整调度与连续通信可行性证据仍在experiment/results和submission。
