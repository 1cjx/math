# 数据清洗说明文档

## 1. 输入范围与总体结论

以用户提供的题面、数据压缩包和结果模板为唯一场景依据，不用外部同类数据替代原始附件。data/raw保留19个原始文件，包含5个基础工作簿、结果模板、DEM/地理辅助文件和题面。SHA-256见results/quality/file_inventory.csv。题面附录1的地理说明文件扩展名与实际附件不同，按实际提供的文件读取并记录，不伪造缺失文件。

全量扫描1,945,174个有效DEM像元、29,980行地理点/顶点及全部业务单元格，执行5,065项检查，失败0项。核心非预期缺失0个，主键重复0个。交付需求为80箱、758.0 kg、2.011 m³。上述结果仅证明本次数据内部一致，不等同于外部测绘或真实飞行验证。

## 2. 数据问题、分布与清洗规则

| 问题/范围 | 数量 | 处理规则 |
|---|---|---|
| NOT_APPLICABLE_DEADLINE / demand.first_deadline_s | 23 | 保留null；增加has_first_deadline，不填0或均值 |
| NOT_APPLICABLE_DEADLINE / boxes.first_deadline_s | 50 | 保留null；以is_first_batch启用约束 |
| MIXED_TABLE_LAYOUT / 5 input workbooks | 5 | 按业务表头解析并转为11张整洁表；保留来源行 |
| MERGED_CELLS / all workbooks | 24 | 只读取定义字段；通信表B:C合并时忽略C列 |
| EMPTY_RESULT_TEMPLATE / 结果提交模板.xlsx | 6 | 填充Q1—Q4六张源表，另附Q3运输与逐箱补充；Q4配置与缺口解释见独立报告 |
| MISSING_NODATA_METADATA / 镇龙乡及周边30米DEM.tif | 1 | 仅补齐NoData元数据，全部高程值保持不变 |
| DEM_STATISTICAL_TAIL / DEM full raster | 99549 | 保留山峰；不截尾、不平滑；输出精确尾部掩码 |
| NODE_DEM_ELEVATION_DIFFERENCE / nodes | 4 | 节点作业高度仍用xlsx；航线净空用DEM；另做全部节点取DEM的敏感性对照 |
| REPEATED_SERVICE_NAMES / services.name | 4 | 以S001—S015唯一ID为主键保留 |
| UNKNOWN_FEATURE_NAME / 镇龙乡及周边水体.csv | 3173 | name置null，name_raw保留原文，并新增name_missing标志 |
| UNKNOWN_FEATURE_NAME / 镇龙乡及周边水系.csv | 3618 | name置null，name_raw保留原文，并新增name_missing标志 |
| UNKNOWN_FEATURE_NAME / 镇龙乡及周边道路.csv | 18518 | name置null，name_raw保留原文，并新增name_missing标志 |
| LEGITIMATE_CLOSURE_POINTS / geospatial rings/roads | 110 | 保留顶点顺序与闭合点；去重检查使用复合主键 |
| HTML_EXTERNAL_RESOURCES / 镇龙乡地理空间详情地图.html | 7 | 保留原始HTML；计算只使用本地数值数据，另输出离线PNG |
| ENERGY_SUBFORMULA_NOT_EXPANDED / 题面 附录2 | 2 | 在解题文档明确列为补充假设：E_hor=E_use*d/L(q)，E_up=(m0+q)gh/(eta*3.6e6)；结果以此口径为条件 |
| GEOSPATIAL_DOCUMENT_EXTENSION / 题面附录1与实际附件 | 1 | 以实际PDF作为附件说明，保留原文件，不视为缺少DEM |

“错误”与“待审核/合理特殊值”分开处理。不是为了清洗而制造改值：本次未删除任何货箱、需求、合法地理顶点或高程；未用均值、截尾或平滑覆盖题定物理值。

## 3. 缺失值及类型格式

需求首批截止时间有23个不适用空值；逐箱首批截止时间有50个不适用空值。这些记录没有相应首批约束。JSON保留null，CSV留空，增加is_first_batch/has_first_deadline。填0会制造任务开始即逾期，均值插补会制造原题没有的时限。医疗expected_s仍为硬时限；首批箱采用first_deadline_s，交集取较早者；其他expected_s作为及时性评价。这些规则自第二问起激活，不提前强加到第一问。

合并单元格从属空白、分区标题、表头和未填结果表是结构信息，不计入样本缺失率。源工作簿按业务表头定位，不将第一行机械当作全部字段。数字按单位转为有限int/float，ID保留字符串，真假值保留布尔；跨表连接使用ID而非地名。UTF-8 BOM CSV便于Excel读取。所有源记录保留文件/表/行定位，字段与单位见data/cleaned/data_dictionary.csv及模型输入JSON。

地理名称“未记录”共影响25,309个顶点行；标准name置空，name_raw保留原文并置name_missing。其坐标与要素仍有效，未知名称不导致删除道路、水体或样本。

## 4. 异常、重复、业务逻辑与分布

统计审核使用1.5倍IQR及修正Z分数（0.67449×偏差/MAD、阈值3.5）；样本不足、MAD=0或IQR=0时不做无意义除法。共有72个“记录×字段”触发标记，它们不等于同等数量的坏样本；完整位置、阈值与原值见statistical_outliers.csv。不同物资质量/体积形成结构化分布，合理差异保留。

所有货箱ID、服务区ID、实体无人机ID、机型ID核查唯一性；需求按服务区和物资汇总，与箱数、总质量、总体积、首批数核对。相同地名不合并，闭环首末重复坐标不按坐标去重；地理重复按要素ID、环、顶点序号判定。机型范围、能量、效率、容量、库存非负/正值、关联外键、医疗/首批时限启用逻辑全部逐项验证。

DEM全局尾部99,549像元按山地分布保留，截峰会破坏巡航净空。GeoTIFF原先未声明NoData，依据同包MAT与说明补上-32767.0，不改变高程数组。节点高程与对应DEM像元差异超过审核阈值共4处，最大绝对差10.562912 m；节点仍用源xlsx高程，航段净空用DEM。MAT/CSV对比222,888单元格，差异0。

## 5. 处理前后及建模入口

| 对象 | 清洗前 | 清洗后 | 变化 |
|---|---|---|---|
| 货箱 | 80 | 80 | 不删除；不改质量、体积、ID |
| 服务区 | 15 | 15 | 不按重名合并 |
| 需求记录 | 53 | 53 | 保持逐类与逐箱一致 |
| 地理点/顶点 | 29980 | 29980 | 保留合法闭合点与顺序 |
| DEM有效像元 | 1945174 | 1945174 | 只修NoData元数据 |
| 题定物理值覆盖 | 0 | 0 | 未人为修正 |
| 逐箱非适用截止时间 | 50 | 50 | 保留null而非插补 |

| 整洁表 | 记录数 |
|---|---|
| depots | 1 |
| services | 15 |
| transport_models | 3 |
| transport_drones | 8 |
| transport_batteries | 3 |
| relay_models | 1 |
| relay_drones | 2 |
| relay_energy | 1 |
| demand | 53 |
| boxes | 80 |
| communication | 14 |
| nodes | 16 |

第二问额外生成transport_battery_units.csv，将原共享电池库存展开为逐个物理资源ID，共14条。这是确定性命名，不是新增库存。每机型编号从01开始，初始SOC=1，同型共享、异型不混用；总库存已经包含初装电池，不再给每架机额外加一组。

数据输入首选data/cleaned/model_inputs.json，空间输入为geospatial/dem_clean.tif或dem_clean.npz；其他地理CSV/MAT的规范视图一并保留。物理建模不进行Z-score标准化；将千米统一为米、时限统一为秒、能耗统一为kWh，而不是破坏量纲后计算。

## 6. 检查文件与边界

field_quality_profiles.csv含逐字段缺失率和分布；business_checks.csv记录逐项判断；issues_and_actions.csv记录问题与处置；node_dem_comparison.csv记录高程差；全量地理和DEM检查结果留在results/quality。清洗不补充题面未定义的能耗分项；相关模型闭合假设在第一、二问报告明示，并做分项扰动。
